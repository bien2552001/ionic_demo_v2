import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DATNK15_PHONE',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
